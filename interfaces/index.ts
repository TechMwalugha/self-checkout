export interface AddNewItemInterface {
    name: string;
    description: string;
    price: number;
    image: string;
    quantity: string;
}

export interface LoadingBtnInterface {
    loading: boolean;
    title: string;
    styles: string;
}